(ns static-roam.templates)

(def template-fns
  {"about"
   (fn [roam-db page-eid])
   "contact"
   (fn [roam-db page-eid])
   "graph-page-example"
   (fn [roam-db page-eid])
   "index"
   (fn [roam-db page-eid])
   "mind"
   (fn [roam-db page-eid])
   "now"
   (fn [roam-db page-eid])
   "post-example"
   (fn [roam-db page-eid])
   "posts"
   (fn [roam-db page-eid])
   })
